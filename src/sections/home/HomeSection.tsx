import SectionHeader from "../../components/SectionHeader";

const HomeSection = () => {
  return (
    <div>
      <SectionHeader>Tovani Hart is now hiring testing</SectionHeader>
    </div>
  );
};

export default HomeSection;
